<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wirelessmobile.menuq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="BaseApplication">
        <activity android:name=".ui.Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ListActivity" />
        <activity android:name=".ui.DetailActivity"></activity>
        <activity android:name=".ui.CartListActivity"></activity>
        <activity android:name=".ui.HomeActivity"></activity>
        <activity android:name=".ui.BillActivity"></activity>
        <activity android:name=".ui.FinishOrderActivity"></activity>
    </application>

</manifest>